import { DatetimeTimezonePipe } from './datetime-timezone.pipe';

describe('DatetimeTimezonePipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeTimezonePipe();
    expect(pipe).toBeTruthy();
  });
});
