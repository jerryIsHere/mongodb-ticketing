import { DatetimeOffsetPipe } from './datetime-offset.pipe';

describe('DatetimeOffsetPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeOffsetPipe();
    expect(pipe).toBeTruthy();
  });
});
