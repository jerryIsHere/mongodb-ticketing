{
    "version": 2,
    "installCommand": "npm install",
    "buildCommand": "npm run build",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/build/index.js"
        }
    ]
}
