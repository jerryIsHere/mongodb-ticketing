{
    "version": 2,
    "installCommand": "npm install && npm install --prefix web",
    "buildCommand": "npm run build",
    "outputDirectory": "build",
    "rewrites": [
        {
            "src": "/(.*)",
            "dest": "/build/index.js"
        }
    ]
}
