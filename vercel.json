{
    "version": 2,
    "installCommand": "npm install && npm install --prefix web",
    "buildCommand": "npm run build",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/build/index.js"
        }
    ]
}
